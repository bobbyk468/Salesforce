#!/bin/sh
# Auto-push current branch to origin after each commit.
# Requires push to be allowed (no auth prompt, or credentials cached).
set -e
branch=$(git symbolic-ref --short HEAD 2>/dev/null) || true
if [ -n "$branch" ]; then
  git push origin "$branch"
fi
