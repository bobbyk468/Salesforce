# Agentforce quizzes — Q-only quote hooks

Quote the **question** tweet (not the answer). Paste the block below as your quote text. Each block is **≤ 280** chars with follow line + hashtags.

**Follow line:** `follow for daily quiz questions + explanations. no spam, just useful.`

---

## Posted — link the question tweet

| Queue id | Question `firstTweetId` | Link |
|----------|-------------------------|------|
| `quiz-agentforce-aiagentsession-q` | `2052252713422086194` | https://x.com/i/web/status/2052252713422086194 |
| `quiz-agentforce-guardrails-q` | `2052252709269762458` | https://x.com/i/web/status/2052252709269762458 |

---

## `quiz-agentforce-aiagentsession-q` (session vs interaction)

**Quote copy:**

```
Data Cloud + Agentforce: they'll ask which object holds the *whole* conversation across agents — not one message, not one turn.

follow for daily quiz questions + explanations. no spam, just useful.

#Agentforce #SalesforceAI
```

---

## `quiz-agentforce-guardrails-q` (CEO health plan leak)

**Quote copy:**

```
Agentforce isn't "just permissions." Sometimes the fix is what the agent is *allowed to talk about* — not what the user can query.

follow for daily quiz questions + explanations. no spam, just useful.

#Agentforce #SalesforceAI
```

---

## New ADM quizzes (May 9 / May 15) — add IDs after first post

After `quiz-adm-validation-cross-field-q` and `quiz-adm-roles-vs-groups-q` are posted, add rows above (or in `quiz-boost-list.md`) with `firstTweetId` links.

**Validation / Closed Won + $0**

```
Classic 201 pattern: "block this save" almost always wants validation — not a formula, not a duplicate rule, and definitely not workflow cosplay.

follow for daily quiz questions + explanations. no spam, just useful.

#ADM201 #SalesforceAdmin
```

**Regional managers / sharing**

```
OWD private + "these people need each other's records but not the world" — brain wants public OWD or view all. usually the boring group + sharing rule is the exam answer.

follow for daily quiz questions + explanations. no spam, just useful.

#ADM201 #SalesforceAdmin
```
